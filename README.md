# password-generator

A programme that generates passwords randomly by combining letters, numbers and special characters together.

Every time the programmes ie excecuted, a password containing letters, numbers and special characters is randomly generated, which is based on the length of the password specified by the user.